import PurchaseDocScreen from '../PurchaseDocScreen';
import { JOB_WORK_PRICE_LIST_CONFIG } from '../purchaseDocConfigs';

export default function JobWorkPriceListPage() {
  return <PurchaseDocScreen config={JOB_WORK_PRICE_LIST_CONFIG} />;
}
